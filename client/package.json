{
  "name": "agent-chat-client",
  "version": "1.0.0",
  "description": "Agent Chat client - connects your AI agent to the chat network",
  "main": "agent-client.js",
  "scripts": {
    "start": "node agent-client.js",
    "test": "node agent-client.js --test",
    "daemon": "pm2 start agent-client.js --name agent-chat && pm2 save"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "node-fetch": "^2.7.0"
  }
}
